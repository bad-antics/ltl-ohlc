// ltl-ohlc/main.ltl
//
// OHLC bar builder with tick aggregation windows.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-ohlc v0.1.0")
    io::println("OHLC bar builder with tick aggregation windows.")
    0
}
