// examples/demo.ltl — runnable demo for ltl_ohlc

import std::io
use ltl_ohlc as lib

fn main() {
    io::println("ltl_ohlc demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}